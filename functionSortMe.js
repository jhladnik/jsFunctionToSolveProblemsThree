// Just a simple sorting usage. Create a function that returns the elements of the input-array / list sorted in lexicographical order.

sortme = function( names ){
    //return the array sorted
    return names.sort()
  }
  
  //given an array of strings
  //return the array of strings with them sorted in lexicographical order
  //input(['baby', 'dog', 'cat']) output(['baby', 'cat', 'dog']); input(['integer', 'string', 'symbol', 'boolean']) output(['boolean', 'integer', 'string', 'symbol'])